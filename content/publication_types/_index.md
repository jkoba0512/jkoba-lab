---
# Disable the /publication_types/ list page itself. Individual term pages
# (e.g. /publication_types/article-journal/) still render and are linked from
# publication pages; only this index is unlinked.
build:
  render: never
  list: never
---

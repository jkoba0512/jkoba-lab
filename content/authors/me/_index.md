---
# Opt back in to rendering: the parent cascade disables every author term page,
# but /people/ links to this one. The title is set here because the term slug
# would otherwise render as "Me" in the browser tab and social previews.
title: 'Jun Kobayashi'
build:
  render: always
  list: local
---

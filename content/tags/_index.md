---
# Disable the /tags/ list page itself. Individual term pages (e.g. /tags/active-inference/)
# still render, because publication pages link to them; only this index is unlinked.
build:
  render: never
  list: never
---
